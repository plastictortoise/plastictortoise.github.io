{
  "name": "simplebinary.github.io",
  "version": "0.0.1",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "rm -rf build && svelte-kit build && touch ./build/.nojekyll",
    "deploy": "npm run build && git subtree split --branch gh-pages --prefix build/",
    "package": "svelte-kit package",
    "preview": "svelte-kit preview"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "next",
    "@sveltejs/adapter-static": "^1.0.0-next.22",
    "@sveltejs/kit": "next",
    "gh-pages": "^3.2.3",
    "svelte": "^3.44.0"
  },
  "type": "module",
  "dependencies": {
    "@fontsource/fira-mono": "^4.5.0",
    "@lukeed/uuid": "^2.0.0",
    "cookie": "^0.4.1"
  }
}
